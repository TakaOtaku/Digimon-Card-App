{
  "rules": {
    "saves": {
      "$uid": {
        ".read": true,
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "tierlist": {
      ".read": true,
      ".write": true,
    }
  }
}
